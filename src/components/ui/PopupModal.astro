---
import CopyButton from '../ui/CopyButton.astro';
import Heading from "../primitives/Heading.astro";
import Text from "../primitives/Text.astro";
import Button from "../primitives/Button.astro";
import Icon from "../primitives/Icon.astro";
import Image from "../primitives/Image.astro";

interface Props {
  data?: {
    active?: boolean;
    title?: string;
    description?: string;
    image?: string;
    coupon_code?: string;
    cta_text?: string;
    denylist?: string[];
  };
}

const { data } = Astro.props;
if (!data?.active) return null;

const denylist = data.denylist || [];
---

<div
  x-data={`popupBehavior('${data.coupon_code}', ${JSON.stringify(denylist)})`}
  x-show="open"
  style="display: none;"
  class="fixed inset-0 flex items-center justify-center px-4 z-[110]"
>
  <div 
    x-show="open"
    x-transition.opacity.duration.300ms
    class="absolute inset-0 bg-black/60 backdrop-blur-sm" 
    @click="closePopup()"
  ></div>

  <div
    x-show="open"
    x-transition:enter="transition ease-out duration-300"
    x-transition:enter-start="opacity-0 translate-y-4 scale-95"
    x-transition:enter-end="opacity-100 translate-y-0 scale-100"
    x-transition:leave="transition ease-in duration-200"
    x-transition:leave-start="opacity-100 translate-y-0 scale-100"
    x-transition:leave-end="opacity-0 translate-y-4 scale-95"
    class="relative bg-white w-full max-w-lg overflow-hidden shadow-2xl rounded-lg z-10"
  >
    <button
      @click="closePopup()"
      class="absolute top-3 right-3 w-8 h-8 flex items-center justify-center p-0 bg-white/80 hover:bg-white rounded-full text-gray-500 hover:text-black transition-colors z-20 shadow-sm cursor-pointer disabled:cursor-not-allowed"
      aria-label="Close"
    >
      <Icon name="PhX" size={16} />
    </button>

    <div class="flex flex-col md:flex-row h-full">
      {data.image && (
        <div class="w-full md:w-2/5 h-48 md:h-auto relative bg-gray-100">
          <Image src={data.image} alt="" class="absolute inset-0 w-full h-full object-cover" />
        </div>
      )}

      <div class="w-full p-6 md:p-8 flex flex-col justify-center" class:list={[data.image ? 'md:w-3/5' : 'md:w-full']}>
        <Heading as="h3" class="mb-2">{data.title}</Heading>
        <Text variant="caption" color="muted" class="mb-6 leading-relaxed">{data.description}</Text>

        {data.coupon_code && (
          <div class="bg-gray-50 border border-gray-200 rounded p-3 mb-6 flex items-center justify-between">
            <Text as="code" variant="code" weight="bold" class="text-lg tracking-widest text-gray-900">{data.coupon_code}</Text>
            <CopyButton text={data.coupon_code} class="underline" />
          </div>
        )}

        {/* Simplified CTA button */}
        <Button
          type="primary"
          @click="applyCoupon()"
          class="w-full py-4 uppercase tracking-widest"
        >
          <Text variant="label" color="inverse">
            {data.cta_text || 'Claim My Discount'}
          </Text>
        </Button>
      </div>
    </div>
  </div>
</div>
