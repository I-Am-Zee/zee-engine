# AI Agent Mistakes Log - 2026-05-02 00:36

## 1. Initialization Order Bug
- **Location**: `src/pages/blog/[...page].astro`
- **Error**: `Cannot access 'headerData' before initialization`
- **Cause**: During the refactoring of the "Featured Post" logic, I moved the access of `headerData.featured_post` to a higher line number than where `headerData` was actually fetched and defined.
- **Impact**: Caused the entire Blog Index to crash with a 500 error in development and build modes.
- **Lesson**: Always verify that frontmatter dependencies (especially async fetches) are fully resolved before any variable access.

## 2. Text Collision Bug (Filtered Count)
- **Location**: `src/components/ui/BlogCategoryFilter.astro`
- **Error**: Visual artifact "SHOWING FILTERED0" (missing space).
- **Cause**: I placed the label and the dynamic count `<span>` side-by-side in the code without an explicit non-breaking space, causing them to touch when rendered.
- **Impact**: Degraded the "Premium" feel of the UI.
- **Lesson**: Use `&nbsp;` or template literals for dynamic text labels to ensure consistent visual padding regardless of CMS input.

## 3. Redundant Border Logic
- **Location**: `src/pages/blog/[...page].astro`
- **Error**: Broken "Full-Bleed" shelf visual.
- **Cause**: I moved the border from the full-bleed Section to the inner container without adding the `border-b` class, making the divider invisible.
- **Impact**: Broke the intended "High-End Shelf" visual architecture.
- **Lesson**: When moving decorative classes (borders, shadows) between nested containers, ensure the utility classes (width, position) are migrated along with the color tokens.

## 4. Layout-Data Entanglement (Section Merge)
- **Location**: src/pages/blog/[...page].astro
- **Error**: Merged the " Page Header\ and \Featured Hero\ into a single <Section variant=\airy\>.
- **Cause**: Misinterpreted a request for \Cover Story\ unity as a requirement for physical container merging.
- **Impact**: Broke the established rhythm of the page, making the transition from title to content feel \soupy\ rather than architectural.
- **Lesson**: Keep distinct page modules in separate Sections unless a shared background/scrim explicitly requires a shared parent.

## 5. Schema Pre-emption (Premature Meta Pollution)
- **Location**: src/content/config.ts
- **Error**: Added a theme field to the global blog article schema for a UI-only featured card toggle.
- **Cause**: Tried to solve a styling contrast problem by modifying the data layer instead of using props or page settings.
- **Impact**: Polluted the content collection with layout-specific metadata that doesn't belong in the \Source of Truth.\
- **Lesson**: Styling states (light/dark) should be handled via Props or Page-level overrides, never by modifying the global content schema.

## 6. Aesthetic Over-reach (Unwanted Hover States)
- **Location**: src/components/ui/BlogCard.astro
- **Error**: Added a group-hover:text-accent-brass override to the featured heading.
- **Cause**: Assumed a decorative color change was needed for interactivity without verifying the brand's monochromatic vision.
- **Impact**: Broke the premium \Monochromatic\ aesthetic requested by the user.
- **Lesson**: For premium/luxury designs, motion (zoom/slide) is often a better feedback mechanism than color changes.

## 7. Constraint \Boxiness\ (Excessive Max-Width)
- **Location**: src/components/ui/BlogCard.astro
- **Error**: Implemented a max-w-[70%] constraint on featured content.
- **Cause**: Attempted to force an \Editorial\ look that ended up making the card feel small and \squeezed\ on large screens.
- **Impact**: Created an imbalanced layout with too much empty space on the right.
- **Lesson**: Start with \Lean\ layouts and only add constraints once the content's natural flow is verified across all breakpoints.
