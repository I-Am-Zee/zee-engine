globalThis.process ??= {}; globalThis.process.env ??= {};
import { b as createAstro, c as createComponent, d as addAttribute, e as renderSlot, f as renderHead, r as renderComponent, a as renderTemplate, g as defineScriptVars, m as maybeRenderHead } from '../../chunks/astro/server_C0Zh7G4i.mjs';
/* empty css                                    */
import { $ as $$Logo } from '../../chunks/Logo_Da1guJ9p.mjs';
/* empty css                                       */
export { renderers } from '../../renderers.mjs';

const $$Astro$1 = createAstro("http://localhost:4321");
const $$CheckoutLayout = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro$1, $$props, $$slots);
  Astro2.self = $$CheckoutLayout;
  const {
    title = "Secure Checkout \u2014 Zelia Vance",
    description = "Complete your purchase securely with Zelia Vance."
  } = Astro2.props;
  return renderTemplate`<html lang="en" data-astro-cid-pd3fick7> <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><meta name="robots" content="noindex, nofollow"><meta name="description"${addAttribute(description, "content")}><link rel="icon" type="image/svg+xml" href="/favicon.svg"><title>${title}</title><!-- Additional per-page head content (e.g. payment SDK scripts) -->${renderSlot($$result, $$slots["head"])}${renderHead()}</head> <body class="checkout-shell" data-astro-cid-pd3fick7> <header class="checkout-header" data-astro-cid-pd3fick7> ${renderComponent($$result, "Logo", $$Logo, { "variant": "full", "data-astro-cid-pd3fick7": true })} <span class="checkout-secure-label" aria-label="This page is secure" data-astro-cid-pd3fick7> <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" data-astro-cid-pd3fick7> <rect x="3" y="11" width="18" height="11" rx="2" ry="2" data-astro-cid-pd3fick7></rect> <path d="M7 11V7a5 5 0 0 1 10 0v4" data-astro-cid-pd3fick7></path> </svg>
Secure Checkout
</span> </header> <main class="checkout-main" data-astro-cid-pd3fick7> ${renderSlot($$result, $$slots["default"])} </main> <footer class="checkout-footer" data-astro-cid-pd3fick7> <p data-astro-cid-pd3fick7>&copy; ${(/* @__PURE__ */ new Date()).getFullYear()} Zelia Vance &mdash; All rights reserved.</p> </footer>  </body> </html>`;
}, "/app/src/layouts/CheckoutLayout.astro", void 0);

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a, _b;
const $$Astro = createAstro("http://localhost:4321");
const prerender = false;
const $$Razorpay = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Razorpay;
  const publicToken = Astro2.url.searchParams.get("publicToken");
  if (!publicToken) {
    return Astro2.redirect("/");
  }
  const razorpayKeyId = "rzp_test_SBluedEOx8ViuP";
  const siteUrl = Astro2.url.origin;
  return renderTemplate`${renderComponent($$result, "CheckoutLayout", $$CheckoutLayout, { "title": "Secure Checkout — Zelia Vance", "data-astro-cid-a4u6wmji": true }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template(["    ", '<div class="checkout-card" data-astro-cid-a4u6wmji> <div class="card-accent" aria-hidden="true" data-astro-cid-a4u6wmji></div> <div class="card-body" data-astro-cid-a4u6wmji> <p class="step-label" data-astro-cid-a4u6wmji>Step 3 of 3</p> <h1 class="card-title" data-astro-cid-a4u6wmji>Payment</h1> <p class="card-subtitle" id="email-display" data-astro-cid-a4u6wmji>Connecting securely&hellip;</p> <!-- Amount — populated after session load --> <div id="amount-section" class="amount-block hidden" aria-live="polite" data-astro-cid-a4u6wmji> <div class="amount-meta" data-astro-cid-a4u6wmji> <span class="amount-label" data-astro-cid-a4u6wmji>Order Total</span> </div> <div class="amount-value" id="amount-display" aria-label="Order total" data-astro-cid-a4u6wmji>&#8377;0</div> </div> <!-- Pay CTA --> <button class="pay-button" id="pay-button" disabled aria-label="Loading payment session" data-astro-cid-a4u6wmji> <span class="loader" aria-hidden="true" data-astro-cid-a4u6wmji> <span class="spinner" data-astro-cid-a4u6wmji></span>\nLoading&hellip;\n</span> </button> <p class="status-text" id="status" role="status" aria-live="polite" data-astro-cid-a4u6wmji></p> <div class="error-box hidden" id="error" role="alert" data-astro-cid-a4u6wmji></div> </div> <!-- Trust strip --> <div class="trust-strip" aria-label="Security information" data-astro-cid-a4u6wmji> <svg width="13" height="13" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" data-astro-cid-a4u6wmji> <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z" data-astro-cid-a4u6wmji></path> </svg>\n256-bit SSL Encryption\n<span class="trust-dot" aria-hidden="true" data-astro-cid-a4u6wmji></span>\nPowered by Razorpay\n<span class="trust-dot" aria-hidden="true" data-astro-cid-a4u6wmji></span>\nPCI DSS Compliant\n</div> </div>  <script>(function(){', '\n    /** @type {object|null} */\n    let paymentSession = null;\n\n    // ── DOM refs ──\n    const payButton     = document.getElementById("pay-button");\n    const emailDisplay  = document.getElementById("email-display");\n    const amountSection = document.getElementById("amount-section");\n    const amountDisplay = document.getElementById("amount-display");\n    const statusEl      = document.getElementById("status");\n    const errorEl       = document.getElementById("error");\n\n    // ── Helpers ──\n    function showError(message, detail = "") {\n      errorEl.textContent = detail ? `${message}: ${detail}` : message;\n      errorEl.classList.remove("hidden");\n    }\n\n    function setStatus(message) {\n      statusEl.textContent = message;\n    }\n\n    function formatINR(amount) {\n      return new Intl.NumberFormat("en-IN", {\n        style: "currency",\n        currency: "INR",\n        minimumFractionDigits: 0,\n      }).format(amount);\n    }\n\n    function setButtonBusy(label) {\n      payButton.disabled = true;\n      payButton.setAttribute("aria-label", label);\n      payButton.innerHTML = `<span class="loader" aria-hidden="true"><span class="spinner"></span>${label}</span>`;\n    }\n\n    function setButtonReady(label) {\n      payButton.disabled = false;\n      payButton.setAttribute("aria-label", label);\n      payButton.textContent = label;\n    }\n\n    // ── Step 1: Fetch Snipcart payment session ──\n    async function fetchPaymentSession() {\n      setStatus("Fetching your order…");\n      const params = new URLSearchParams({ publicToken });\n      const url = `https://payment.snipcart.com/api/public/custom-payment-gateway/payment-session?${params}`;\n\n      const response = await fetch(url);\n      if (!response.ok) {\n        const body = await response.text().catch(() => "");\n        throw new Error(`Session fetch failed (${response.status})${body ? `: ${body}` : ""}`);\n      }\n\n      paymentSession = await response.json();\n\n      // Populate UI\n      emailDisplay.textContent = paymentSession.invoice.email || "Verified customer";\n      amountDisplay.textContent = formatINR(paymentSession.invoice.amount);\n      amountSection.classList.remove("hidden");\n    }\n\n    // ── Step 2: Open Razorpay Standard Checkout ──\n    function openRazorpay() {\n      setStatus("Opening secure payment window…");\n\n      const amountInPaise = Math.round(paymentSession.invoice.amount * 100);\n\n      const options = {\n        key: razorpayKeyId,\n        amount: amountInPaise,\n        currency: "INR",\n        name: "Zelia Vance",\n        description: `Order #${String(paymentSession.id).substring(0, 8).toUpperCase()}`,\n        image: `${siteUrl}/favicon.svg`,\n        prefill: {\n          name:    paymentSession.invoice.billingAddress?.name || "",\n          email:   paymentSession.invoice.email               || "",\n          contact: "",\n        },\n        theme: { color: "#052b22" },\n        modal: {\n          ondismiss() {\n            setStatus("Payment cancelled. Click below to try again.");\n            setButtonReady("Pay Now");\n          },\n        },\n        handler: async (response) => {\n          await verifyPayment(response);\n        },\n      };\n\n      const rzp = new Razorpay(options);\n      rzp.on("payment.failed", (ev) => {\n        showError("Payment failed", ev.error?.description || "Unknown error");\n        setButtonReady("Try Again");\n      });\n      rzp.open();\n    }\n\n    // ── Step 3: Verify & confirm with Snipcart ──\n    async function verifyPayment(razorpayResponse) {\n      setButtonBusy("Verifying…");\n      setStatus("Confirming your payment…");\n\n      const payload = {\n        razorpay_payment_id: razorpayResponse.razorpay_payment_id,\n        razorpay_order_id:   razorpayResponse.razorpay_order_id  || "",\n        razorpay_signature:  razorpayResponse.razorpay_signature  || "",\n        paymentSessionId: paymentSession.id,\n        amount:   paymentSession.invoice.amount,\n        currency: paymentSession.invoice.currency,\n      };\n\n      const res = await fetch("/api/checkout/verify", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify(payload),\n      });\n      const result = await res.json();\n\n      if (result.success) {\n        setStatus("✓ Payment confirmed! Redirecting…");\n        payButton.textContent = "✓ Payment Complete";\n        const redirect = result.redirectUrl\n          || paymentSession.paymentAuthorizationRedirectUrl\n          || "/";\n        window.location.href = redirect;\n      } else {\n        throw new Error(result.error || "Verification failed");\n      }\n    }\n\n    // ── Boot ──\n    async function init() {\n      if (!razorpayKeyId) {\n        showError("Configuration error — contact support");\n        return;\n      }\n      try {\n        await fetchPaymentSession();\n        setButtonReady("Pay Now");\n        setStatus("");\n        payButton.onclick = () => {\n          setButtonBusy("Opening…");\n          openRazorpay();\n        };\n      } catch (err) {\n        showError("Unable to load your order. The session may have expired.", err?.message);\n        payButton.disabled = true;\n        payButton.textContent = "Session Expired";\n      }\n    }\n\n    init();\n  })();</script> '], ["    ", '<div class="checkout-card" data-astro-cid-a4u6wmji> <div class="card-accent" aria-hidden="true" data-astro-cid-a4u6wmji></div> <div class="card-body" data-astro-cid-a4u6wmji> <p class="step-label" data-astro-cid-a4u6wmji>Step 3 of 3</p> <h1 class="card-title" data-astro-cid-a4u6wmji>Payment</h1> <p class="card-subtitle" id="email-display" data-astro-cid-a4u6wmji>Connecting securely&hellip;</p> <!-- Amount — populated after session load --> <div id="amount-section" class="amount-block hidden" aria-live="polite" data-astro-cid-a4u6wmji> <div class="amount-meta" data-astro-cid-a4u6wmji> <span class="amount-label" data-astro-cid-a4u6wmji>Order Total</span> </div> <div class="amount-value" id="amount-display" aria-label="Order total" data-astro-cid-a4u6wmji>&#8377;0</div> </div> <!-- Pay CTA --> <button class="pay-button" id="pay-button" disabled aria-label="Loading payment session" data-astro-cid-a4u6wmji> <span class="loader" aria-hidden="true" data-astro-cid-a4u6wmji> <span class="spinner" data-astro-cid-a4u6wmji></span>\nLoading&hellip;\n</span> </button> <p class="status-text" id="status" role="status" aria-live="polite" data-astro-cid-a4u6wmji></p> <div class="error-box hidden" id="error" role="alert" data-astro-cid-a4u6wmji></div> </div> <!-- Trust strip --> <div class="trust-strip" aria-label="Security information" data-astro-cid-a4u6wmji> <svg width="13" height="13" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" data-astro-cid-a4u6wmji> <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z" data-astro-cid-a4u6wmji></path> </svg>\n256-bit SSL Encryption\n<span class="trust-dot" aria-hidden="true" data-astro-cid-a4u6wmji></span>\nPowered by Razorpay\n<span class="trust-dot" aria-hidden="true" data-astro-cid-a4u6wmji></span>\nPCI DSS Compliant\n</div> </div>  <script>(function(){', '\n    /** @type {object|null} */\n    let paymentSession = null;\n\n    // ── DOM refs ──\n    const payButton     = document.getElementById("pay-button");\n    const emailDisplay  = document.getElementById("email-display");\n    const amountSection = document.getElementById("amount-section");\n    const amountDisplay = document.getElementById("amount-display");\n    const statusEl      = document.getElementById("status");\n    const errorEl       = document.getElementById("error");\n\n    // ── Helpers ──\n    function showError(message, detail = "") {\n      errorEl.textContent = detail ? \\`\\${message}: \\${detail}\\` : message;\n      errorEl.classList.remove("hidden");\n    }\n\n    function setStatus(message) {\n      statusEl.textContent = message;\n    }\n\n    function formatINR(amount) {\n      return new Intl.NumberFormat("en-IN", {\n        style: "currency",\n        currency: "INR",\n        minimumFractionDigits: 0,\n      }).format(amount);\n    }\n\n    function setButtonBusy(label) {\n      payButton.disabled = true;\n      payButton.setAttribute("aria-label", label);\n      payButton.innerHTML = \\`<span class="loader" aria-hidden="true"><span class="spinner"></span>\\${label}</span>\\`;\n    }\n\n    function setButtonReady(label) {\n      payButton.disabled = false;\n      payButton.setAttribute("aria-label", label);\n      payButton.textContent = label;\n    }\n\n    // ── Step 1: Fetch Snipcart payment session ──\n    async function fetchPaymentSession() {\n      setStatus("Fetching your order…");\n      const params = new URLSearchParams({ publicToken });\n      const url = \\`https://payment.snipcart.com/api/public/custom-payment-gateway/payment-session?\\${params}\\`;\n\n      const response = await fetch(url);\n      if (!response.ok) {\n        const body = await response.text().catch(() => "");\n        throw new Error(\\`Session fetch failed (\\${response.status})\\${body ? \\`: \\${body}\\` : ""}\\`);\n      }\n\n      paymentSession = await response.json();\n\n      // Populate UI\n      emailDisplay.textContent = paymentSession.invoice.email || "Verified customer";\n      amountDisplay.textContent = formatINR(paymentSession.invoice.amount);\n      amountSection.classList.remove("hidden");\n    }\n\n    // ── Step 2: Open Razorpay Standard Checkout ──\n    function openRazorpay() {\n      setStatus("Opening secure payment window…");\n\n      const amountInPaise = Math.round(paymentSession.invoice.amount * 100);\n\n      const options = {\n        key: razorpayKeyId,\n        amount: amountInPaise,\n        currency: "INR",\n        name: "Zelia Vance",\n        description: \\`Order #\\${String(paymentSession.id).substring(0, 8).toUpperCase()}\\`,\n        image: \\`\\${siteUrl}/favicon.svg\\`,\n        prefill: {\n          name:    paymentSession.invoice.billingAddress?.name || "",\n          email:   paymentSession.invoice.email               || "",\n          contact: "",\n        },\n        theme: { color: "#052b22" },\n        modal: {\n          ondismiss() {\n            setStatus("Payment cancelled. Click below to try again.");\n            setButtonReady("Pay Now");\n          },\n        },\n        handler: async (response) => {\n          await verifyPayment(response);\n        },\n      };\n\n      const rzp = new Razorpay(options);\n      rzp.on("payment.failed", (ev) => {\n        showError("Payment failed", ev.error?.description || "Unknown error");\n        setButtonReady("Try Again");\n      });\n      rzp.open();\n    }\n\n    // ── Step 3: Verify & confirm with Snipcart ──\n    async function verifyPayment(razorpayResponse) {\n      setButtonBusy("Verifying…");\n      setStatus("Confirming your payment…");\n\n      const payload = {\n        razorpay_payment_id: razorpayResponse.razorpay_payment_id,\n        razorpay_order_id:   razorpayResponse.razorpay_order_id  || "",\n        razorpay_signature:  razorpayResponse.razorpay_signature  || "",\n        paymentSessionId: paymentSession.id,\n        amount:   paymentSession.invoice.amount,\n        currency: paymentSession.invoice.currency,\n      };\n\n      const res = await fetch("/api/checkout/verify", {\n        method: "POST",\n        headers: { "Content-Type": "application/json" },\n        body: JSON.stringify(payload),\n      });\n      const result = await res.json();\n\n      if (result.success) {\n        setStatus("✓ Payment confirmed! Redirecting…");\n        payButton.textContent = "✓ Payment Complete";\n        const redirect = result.redirectUrl\n          || paymentSession.paymentAuthorizationRedirectUrl\n          || "/";\n        window.location.href = redirect;\n      } else {\n        throw new Error(result.error || "Verification failed");\n      }\n    }\n\n    // ── Boot ──\n    async function init() {\n      if (!razorpayKeyId) {\n        showError("Configuration error — contact support");\n        return;\n      }\n      try {\n        await fetchPaymentSession();\n        setButtonReady("Pay Now");\n        setStatus("");\n        payButton.onclick = () => {\n          setButtonBusy("Opening…");\n          openRazorpay();\n        };\n      } catch (err) {\n        showError("Unable to load your order. The session may have expired.", err?.message);\n        payButton.disabled = true;\n        payButton.textContent = "Session Expired";\n      }\n    }\n\n    init();\n  })();</script> '])), maybeRenderHead(), defineScriptVars({ publicToken, razorpayKeyId, siteUrl })), "head": async ($$result2) => renderTemplate(_b || (_b = __template(['<script src="https://checkout.razorpay.com/v1/checkout.js"></script>']))) })} `;
}, "/app/src/pages/checkout/razorpay.astro", void 0);
const $$file = "/app/src/pages/checkout/razorpay.astro";
const $$url = "/checkout/razorpay";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Razorpay,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
