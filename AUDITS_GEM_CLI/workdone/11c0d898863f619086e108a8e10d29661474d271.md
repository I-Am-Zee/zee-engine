# Commit Summary: 11c0d898863f619086e108a8e10d29661474d271
**Message:** chore: stabilize codebase, fix compiler crash and schema errors

## What was done
1. **Critical Syntax Fix (Compiler Crash):**
   - Restored the missing opening `---` delimiter in `src/pages/newsletter/confirm.astro`. This was causing a fatal panic in the Astro compiler (`panic: html: bad parser state: originalIM was set twice`), which effectively broke the dev server and build process.

2. **Content Mapping Alignment:**
   - Corrected `getEntry` calls in `src/pages/newsletter/confirm.astro` and `src/pages/newsletter/success.astro`. They were previously attempting to fetch from non-existent collections. I mapped them to the `pages_content` collection where the entries actually reside.

3. **Keystatic Schema Stabilization:**
   - **`keystatic.config.ts`**: Fixed TypeScript errors by adding missing `defaultValue` properties to `fields.select` blocks.
   - **Navigation Sync**: Updated the `navigation` object in the Keystatic config to match the actual collection and singleton keys (e.g., adding `page_checkout_razorpay` and `settings_store_checkout`), ensuring the CMS sidebar remains functional.

4. **Component Prop & Interface Cleanup:**
   - **`AffiliateProductCTA.astro`**: Added a spread operator `{...rest}` and updated the `Props` interface to accept `any` keys, allowing Alpine.js attributes like `x-bind:links` to be passed without causing build-time errors in `AffiliateQuickView.astro`.
   - **`Image.astro`**: Narrowed the `draggable` prop type to `boolean | "true" | "false"` to resolve a type mismatch with Astro's image assets.
   - **`PriceTag.astro`**: Handled `undefined` values for `salePrice` in the `calculateDiscount` utility to prevent `NaN` errors.
   - **`SideDrawer.astro`**: Fixed a duplicated `alt` attribute on the `<Image />` component.

5. **Commit Scope:**
   - Staged and committed the `AUDITS_GEM_CLI/workdone/` documentation folder to preserve the audit trail within the repository history.

## Why it was done
These changes were performed to restore the codebase to a stable, buildable state following a "blunder" identified via `npx astro check`. The primary goal was to fix the fatal compiler error and ensure that all new components (Tracking, CTAs, and Design Tokens) integrated safely with the existing content layer and CMS schema.
