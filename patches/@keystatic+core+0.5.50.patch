diff --git a/node_modules/@keystatic/core/dist/read-local-b6b71900.node.react-server.js b/node_modules/@keystatic/core/dist/read-local-b6b71900.node.react-server.js
index cc6c2cb..019c5e9 100644
--- a/node_modules/@keystatic/core/dist/read-local-b6b71900.node.react-server.js
+++ b/node_modules/@keystatic/core/dist/read-local-b6b71900.node.react-server.js
@@ -1,4 +1,5 @@
 import fs from 'fs/promises';
+import nodeFs from 'node:fs';
 import path from 'path';
 import { b as getDirectoriesForTreeKey, c as getCollectionPath, o as object, e as getSingletonFormat, h as getSingletonPath, u as updateTreeWithChanges, i as blobSha } from './index-ca270311.node.react-server.js';
 import 'react/jsx-runtime';
@@ -23,43 +24,43 @@ async function readDirEntries(dir) {
 async function collectEntriesInDir(baseDir, ancestors) {
   const currentRelativeDir = ancestors.map(p => p.segment).join('/');
   const entries = await readDirEntries(path.join(baseDir, currentRelativeDir));
-  const gitignore = entries.find(entry => entry.isFile() && entry.name === '.gitignore');
-  const gitignoreFilterForDescendents = gitignore ? ignore().add(await fs.readFile(path.join(baseDir, currentRelativeDir, gitignore.name), 'utf8')).createFilter() : () => true;
   const pathSegments = ancestors.map(x => x.segment);
   return (await Promise.all(entries.filter(entry => {
-    if (!entry.isDirectory() && !entry.isFile() || entry.name === '.git' || entry.name === 'node_modules' || entry.name === '.next') {
-      return false;
+    const fullPath = path.join(baseDir, currentRelativeDir, entry.name);
+    let isDir = entry.isDirectory();
+    let isFile = entry.isFile();
+    if (entry.isSymbolicLink()) {
+      try {
+        const stat = nodeFs.statSync(fullPath);
+        isDir = stat.isDirectory();
+        isFile = stat.isFile();
+      } catch (e) {}
     }
-    const innerPath = `${pathSegments.concat(entry.name).join('/')}${entry.isDirectory() ? '/' : ''}`;
-    if (!gitignoreFilterForDescendents(innerPath)) {
+    if (!isDir && !isFile || entry.name === '.git' || entry.name === 'node_modules' || entry.name === '.next') {
       return false;
     }
-    let currentPath = entry.name;
-    for (let i = ancestors.length - 1; i >= 0; i--) {
-      const ancestor = ancestors[i];
-      currentPath = `${ancestor.segment}/${currentPath}`;
-      if (!ancestor.gitignoreFilterForDescendents(currentPath)) {
-        return false;
-      }
-    }
     return true;
   }).map(async entry => {
-    if (entry.isDirectory()) {
+    const fullPath = path.join(baseDir, currentRelativeDir, entry.name);
+    let isDir = entry.isDirectory();
+    if (entry.isSymbolicLink()) {
+      try {
+        isDir = nodeFs.statSync(fullPath).isDirectory();
+      } catch (e) {}
+    }
+    if (isDir) {
       return collectEntriesInDir(baseDir, [...ancestors, {
-        gitignoreFilterForDescendents,
         segment: entry.name
       }]);
-    } else {
-      const innerPath = pathSegments.concat(entry.name).join('/');
-      const contents = await fs.readFile(path.join(baseDir, innerPath));
-      return {
-        path: innerPath,
-        contents: {
-          byteLength: contents.byteLength,
-          sha: await blobSha(contents)
-        }
-      };
     }
+    const contents = await fs.readFile(fullPath);
+    return {
+      path: pathSegments.concat(entry.name).join('/'),
+      contents: {
+        byteLength: contents.byteLength,
+        sha: await blobSha(contents)
+      }
+    };
   }))).flat();
 }
 async function readToDirEntries(baseDir) {
diff --git a/node_modules/@keystatic/core/dist/read-local-bc5599c1.node.js b/node_modules/@keystatic/core/dist/read-local-bc5599c1.node.js
index 7756c22..874e033 100644
--- a/node_modules/@keystatic/core/dist/read-local-bc5599c1.node.js
+++ b/node_modules/@keystatic/core/dist/read-local-bc5599c1.node.js
@@ -1,4 +1,5 @@
 import fs from 'fs/promises';
+import nodeFs from 'node:fs';
 import path from 'path';
 import { b as getDirectoriesForTreeKey, c as getCollectionPath, o as object, e as getSingletonFormat, h as getSingletonPath, u as updateTreeWithChanges, i as blobSha } from './index-360e5820.node.js';
 import 'react/jsx-runtime';
@@ -23,43 +24,43 @@ async function readDirEntries(dir) {
 async function collectEntriesInDir(baseDir, ancestors) {
   const currentRelativeDir = ancestors.map(p => p.segment).join('/');
   const entries = await readDirEntries(path.join(baseDir, currentRelativeDir));
-  const gitignore = entries.find(entry => entry.isFile() && entry.name === '.gitignore');
-  const gitignoreFilterForDescendents = gitignore ? ignore().add(await fs.readFile(path.join(baseDir, currentRelativeDir, gitignore.name), 'utf8')).createFilter() : () => true;
   const pathSegments = ancestors.map(x => x.segment);
   return (await Promise.all(entries.filter(entry => {
-    if (!entry.isDirectory() && !entry.isFile() || entry.name === '.git' || entry.name === 'node_modules' || entry.name === '.next') {
-      return false;
+    const fullPath = path.join(baseDir, currentRelativeDir, entry.name);
+    let isDir = entry.isDirectory();
+    let isFile = entry.isFile();
+    if (entry.isSymbolicLink()) {
+      try {
+        const stat = nodeFs.statSync(fullPath);
+        isDir = stat.isDirectory();
+        isFile = stat.isFile();
+      } catch (e) {}
     }
-    const innerPath = `${pathSegments.concat(entry.name).join('/')}${entry.isDirectory() ? '/' : ''}`;
-    if (!gitignoreFilterForDescendents(innerPath)) {
+    if (!isDir && !isFile || entry.name === '.git' || entry.name === 'node_modules' || entry.name === '.next') {
       return false;
     }
-    let currentPath = entry.name;
-    for (let i = ancestors.length - 1; i >= 0; i--) {
-      const ancestor = ancestors[i];
-      currentPath = `${ancestor.segment}/${currentPath}`;
-      if (!ancestor.gitignoreFilterForDescendents(currentPath)) {
-        return false;
-      }
-    }
     return true;
   }).map(async entry => {
-    if (entry.isDirectory()) {
+    const fullPath = path.join(baseDir, currentRelativeDir, entry.name);
+    let isDir = entry.isDirectory();
+    if (entry.isSymbolicLink()) {
+      try {
+        isDir = nodeFs.statSync(fullPath).isDirectory();
+      } catch (e) {}
+    }
+    if (isDir) {
       return collectEntriesInDir(baseDir, [...ancestors, {
-        gitignoreFilterForDescendents,
         segment: entry.name
       }]);
-    } else {
-      const innerPath = pathSegments.concat(entry.name).join('/');
-      const contents = await fs.readFile(path.join(baseDir, innerPath));
-      return {
-        path: innerPath,
-        contents: {
-          byteLength: contents.byteLength,
-          sha: await blobSha(contents)
-        }
-      };
     }
+    const contents = await fs.readFile(fullPath);
+    return {
+      path: pathSegments.concat(entry.name).join('/'),
+      contents: {
+        byteLength: contents.byteLength,
+        sha: await blobSha(contents)
+      }
+    };
   }))).flat();
 }
 async function readToDirEntries(baseDir) {
