Summary:
Completed the "Perfected Breadcrumbs" implementation and site-wide cleanup. 
1. Fixed `Link.astro` to use correct Tailwind v4 token syntax.
2. Refactored `Breadcrumbs.astro` to include its own layout bar and alignment logic, using `variant="metadata"` for current items and `!` overrides to ensure visual purity.
3. Removed redundant breadcrumbs from top-level pages (Shop All, Collections Index, Search, Wishlist, Design System).
4. Verified horizontal alignment with `chrome-devtools` (identical rects).
---
Full Log:
- **Link.astro Fix:** Corrected legacy v3 classes (`text-text-main`, `hover:text-text-muted`) to v4 token syntax (`text-(--color-text-main)`, `hover:text-(--color-text-muted)`).
- **Breadcrumbs.astro Refactor:**
  - Added `py-4` for better vertical breathing room.
  - Used `variant="metadata"` for current page text to ensure perfect typography from the primitive.
  - Applied `!` modifiers (`no-underline!`, `text-(--color-text-muted)!`, `hover:text-(--color-text-main)!`) to guarantee intended styles aren't overridden by base primitive styles.
  - Softened the bottom border to `border-(--color-border-subtle)/30`.
- **Cleanup:** Removed the `<Breadcrumbs />` component from pages where it was redundant or misapplied by previous agents (Index pages, Search, Wishlist).
- **Verification:** Used Chrome DevTools to confirm that breadcrumbs now align perfectly with standard site content containers (`max-w-7xl px-4 lg:px-8`).
